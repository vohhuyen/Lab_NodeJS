doctype html
html
  head
    title Iteration Example
  body
    h2 Each Example
    ul
      each val in [1, 2, 3, 4, 5]
        li= val

    h2 Each with Index Example
    ul
      each val, index in ['zero', 'one', 'two']
        li= index + ': ' + val

    h2 Each with Object Example
    ul
      each val, index in {1:'one', 2:'two', 3:'three'}
        li= index + ': ' + val

    h2 Each with Conditional Example
    - var values = [];
    ul
      each val in values.length ? values : ['There are no values']
        li= val

    h2 While Example
    - var n = 0
    ul
      while n < 4
        li= n++
